// Navigation function for redirecting to different pages
function navigateTo(page) {
    if (page === 'signup') {
        window.location.href = 'signup.html'; // Redirect to Sign-Up page
    } else if (page === 'about') {
        window.location.href = 'about.html'; // Redirect to About Us page
    }
}
